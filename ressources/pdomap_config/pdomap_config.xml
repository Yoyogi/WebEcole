<?xml version="1.0"?>
<pdomap>
    <connections>
        <add key="1"
        dsn="mysql:host=localhost;dbname=webecole"
        user="root"
        pwd=""
        prefix=""/>
    </connections>
    <!-- the mapping definition starts here -->
    <mapping root="./">
        <adapter name="students" meta="student.map.xml"/>
    </mapping>
</pdomap>
